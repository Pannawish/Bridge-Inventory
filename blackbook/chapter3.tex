\chapter{METHODOLOGY}

This chapter explains how the Inventory Management web application was developed. The focus is on the development approach, architecture, workflow design, validation method, testing method, and tools used in the project. In this project, we built the system as a React web application connected to a Django REST Framework backend and a MySQL database.

\section{Development Approach}
We developed the system step by step, starting from the data that every other workflow depends on. The first part was master data, such as products, categories, suppliers, and customers. After that, we developed the transaction modules: purchases, sales, and quotations. The finance-related modules, including billing notes, payment batches, and credit notes, were added after the main transactions were already working. Finally, we connected the dashboard, inventory workspace, AI pages, administration pages, and language settings to the backend-supported workflow.

The project was separated into three main layers:

\begin{enumerate}
	\item \textbf{Frontend layer:} React and Vite were used to build the web pages, forms, filters, tables, modals, and tab navigation.
	\item \textbf{Backend layer:} Django REST Framework was used to expose APIs, validate data, calculate stock, control eligibility rules, prepare dashboard and assistant responses, and support AI report generation when an API key is configured.
	\item \textbf{Database layer:} MySQL was used through the Django ORM to store relational business records.
\end{enumerate}

This structure helped keep the user interface separate from the business rules. The frontend guides the user, but the backend is still responsible for the important checks such as stock validation, transaction eligibility, and payable amount synchronization.

\section{Requirement Areas and Web-App Pages}
The web application is organized around the main work areas that a trading business needs each day. Each sidebar page has a specific purpose, and the pages are connected through shared product, supplier, customer, purchase, sale, and finance data.

Tables~\ref{tab:requirement-page-map} and~\ref{tab:record-support-page-map} describe how the business requirements are divided among the implemented pages. Table~\ref{tab:requirement-page-map} covers the main operational pages, while Table~\ref{tab:record-support-page-map} continues with record management, administration, and language support.

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\caption{Requirement Areas and Implemented Pages}
	\label{tab:requirement-page-map}
	\begin{tabularx}{\textwidth}{|B{2.8cm}|B{2.8cm}|Z|}
		\hline
		\textbf{Requirement Area} & \textbf{Web-App Page} & \textbf{Purpose in the System} \\ \hline
		Operation summary & Dashboard & Shows stock, finance, cashflow, product movement, and order coverage summaries. \\ \hline
		Inventory control & Inventory & Shows stock health, reorder information, supplier options, product details, and quick purchase-order preparation. \\ \hline
		Assistant support & AI Chat & Answers supported read-only questions from verified backend facts; configured AI responses add a conclusion and question-specific highlights. \\ \hline
		Report generation & AI Report & Generates printable supplier, customer, or product reports with metrics, charts, record tables, and AI-written analysis. \\ \hline
		Purchasing & Purchases & Records purchase orders, line items, receiving status, documents, VAT totals, discounts, and payable values. \\ \hline
		Supplier payment & Payment Batches & Groups eligible purchases for supplier payment and tracks paid or unpaid lines. \\ \hline
		Quotation & Quotation & Prepares quotations with line items, supplier options, validity dates, and conversion to purchase or sale records. \\ \hline
		Sales & Sales & Records customer sales, stock allocation, delivery status, documents, VAT totals, and credit-note links. \\ \hline
	\end{tabularx}
\end{table}

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\caption{Record and Support Page Scope}
	\label{tab:record-support-page-map}
	\begin{tabularx}{\textwidth}{|B{2.8cm}|B{2.8cm}|Z|}
		\hline
		\textbf{Requirement Area} & \textbf{Web-App Page} & \textbf{Purpose in the System} \\ \hline
		Customer billing & Billing Notes & Groups eligible sales into customer billing notes and tracks received or outstanding lines. \\ \hline
		Returns and cancellation adjustment & Credit Notes & Creates credit notes from eligible cancelled or returned sale items. \\ \hline
		Product records & Products & Manages product details, SKUs, image/PDF attachments, units, reorder levels, active status, suppliers, margin information, and history. \\ \hline
		Category records & Categories & Manages product categories using a parent-child category tree. \\ \hline
		Supplier records & Supplier & Manages supplier profile, contact information, procurement contact, branches, shipping addresses, and terms. \\ \hline
		Customer records & Customer & Manages customer profile, contact information, branches, shipping addresses, and billing terms. \\ \hline
		Administration and audit & User Access / Activity Log & Manages users, roles, and permissions, and provides authorized review of system activity. \\ \hline
		Language preference & Settings & Switches the application interface between English and Thai. \\ \hline
	\end{tabularx}
\end{table}

\section{System Architecture}
The system uses a three-tier web architecture. React is responsible for the user interface, Django REST Framework provides the API and business rules, and MySQL stores the relational records. The frontend does not connect directly to the database. Every important action goes through the API.

Figure~\ref{fig:system-architecture-overview} shows the direction of communication between the user interface, API, business services, database, and read-only assistant.

\begin{figure}[H]
\centering
\resizebox{0.68\textwidth}{!}{
\begin{tikzpicture}[
	>=Stealth,
	font=\small,
	mainbox/.style={rectangle, rounded corners=2pt, draw=black, thick, align=center, text width=5.1cm, minimum height=1cm, fill=gray!10},
	servicebox/.style={rectangle, rounded corners=2pt, draw=black, align=center, text width=5.1cm, minimum height=1cm, fill=blue!8},
	databox/.style={rectangle, rounded corners=2pt, draw=black, align=center, text width=5.1cm, minimum height=1cm, fill=green!10},
	sidebox/.style={rectangle, rounded corners=2pt, draw=black, dashed, align=center, text width=5.1cm, minimum height=0.95cm, fill=yellow!12}
]
	\node[mainbox] (frontend) at (0,0) {React + Vite Frontend};
	\node[mainbox] (api) at (0,-2.1) {Django REST Framework API\\JWT Login and User Access};
	\node[servicebox] (services) at (0,-4.2) {Backend Services\\Validation, Stock, Finance};
	\node[databox] (mysql) at (0,-6.3) {MySQL Database};
	\node[sidebox] (assistant) at (6.4,-6.3) {Read-only AI Assistant\\Supported AI Query Workflows};
	
	\draw[<->, thick] (frontend) -- (api);
	\draw[<->, thick] (api) -- (services);
	\draw[<->, thick] (services) -- (mysql);
	\draw[<->, dashed, thick] (services.east) -- (assistant.west);
\end{tikzpicture}
}
\vspace{1em}
\caption{System Architecture Overview}
\label{fig:system-architecture-overview}
\end{figure}

Table~\ref{tab:architecture-components} explains the responsibility and implementation used for each architecture layer shown in Figure~\ref{fig:system-architecture-overview}.

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\caption{Architecture Components}
	\label{tab:architecture-components}
	\begin{tabularx}{\textwidth}{|B{2.8cm}|B{3cm}|Z|}
		\hline
		\textbf{Layer} & \textbf{Implementation} & \textbf{Main Responsibility} \\ \hline
		Frontend & React 18 and Vite & Builds the tabbed interface, forms, filters, tables, modals, and bilingual text display. \\ \hline
		API & Django REST Framework & Receives requests, serializes data, applies filters, supports pagination, and returns JSON responses. \\ \hline
		Business logic & Django service functions & Handles stock calculation, FIFO allocation, dashboard summaries, payable synchronization, eligibility checks, and assistant context. \\ \hline
		Database & MySQL with Django ORM & Stores master data, transaction records, line items, documents, allocations, and finance records. \\ \hline
		Authentication and access control & Simple JWT, Django groups, and Django permissions & Provides login, refresh token, authenticated profile support, role-based user access, and permission-gated API actions. \\ \hline
	\end{tabularx}
\end{table}

\section{Frontend Methodology}
The frontend was developed with React components, hooks, and helper files. Large pages are split into smaller sections, such as form details, line items, totals, directory tables, and detail modals. This made the code easier to manage because a purchase form, for example, has different concerns from a purchase history table.

The API client in \texttt{frontend/src/api.js} centralizes communication with the backend. It builds query strings, sends JSON or form-data requests, attaches bearer tokens, retries a request after token refresh, and dispatches an authentication-expired event when refresh fails. The frontend permission helper uses the profile returned from \texttt{/api/auth/me/} to show or hide navigation tabs such as products, purchases, sales, user access, and activity log. The data-loading hooks keep full lookup data separate from paginated directory rows, so forms can still access reference data while history pages can use pagination. User-facing text is selected from English and Thai dictionaries through the language context, and the Settings page controls the active interface language.

The frontend also keeps a mock-data fallback for development and demonstration. This fallback is useful when the backend is not running, but it is not treated as the final authority. The backend remains the source of truth for stock validation, eligibility, and financial synchronization.

\section{Backend and API Methodology}
The backend was developed as a Django project with one main inventory application. Django REST Framework viewsets provide CRUD endpoints for products, categories, suppliers, customers, purchases, sales, quotations, billing notes, payment batches, and credit notes. Separate API views provide dashboard summaries, dashboard segments, lookup lists, product history, product stock layers, eligibility lists, and chat responses.

Table~\ref{tab:api-endpoint-groups} summarizes the main endpoint groups and the part of the application served by each group.

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\renewcommand{\arraystretch}{1.45}
	\caption{Main API Endpoint Groups}
	\label{tab:api-endpoint-groups}
	\begin{tabularx}{\textwidth}{|B{4cm}|Z|}
		\hline
		\textbf{Endpoint Group} & \textbf{Purpose} \\ \hline
		\texttt{/api/products/}, \texttt{/api/categories/} & Product catalog and category tree management. \\ \hline
		\texttt{/api/suppliers/}, \texttt{/api/customers/} & Business partner master data management. \\ \hline
		\texttt{/api/purchases/}, \texttt{/api/sales/} & Purchase and sales transaction workflows, including line items and documents. \\ \hline
		\texttt{/api/quotations/} & Quotation records with normalized line items and supplier options. \\ \hline
		\texttt{/api/billing-notes/}, \texttt{/api/payment-batches/}, \texttt{/api/credit-notes/} & Customer receivable, supplier payable, and credit note workflows. \\ \hline
		\texttt{/api/eligibility/...} & Backend-controlled lists for billing-note sales, payment-batch purchases, and credit-note sale lines. \\ \hline
		\texttt{/api/dashboard/}, \texttt{/api/dashboard/segment/} & Dashboard summary and segmented operational reports. \\ \hline
		\texttt{/api/products/<id>/history/} and \texttt{/stock-layers/} & Product transaction history and received stock layer details. \\ \hline
		\texttt{/api/chat/} & Read-only assistant answers from backend-prepared facts. Configured OpenAI responses include a conclusion and highlights; failures use the local summary. \\ \hline
		\texttt{/api/ai-reports/} & Generates printable HTML reports for a selected supplier, customer, or product with metrics, charts, and record tables. \\ \hline
		\texttt{/api/auth/login/}, \texttt{/refresh/}, \texttt{/me/} & Login, token refresh, and authenticated user profile. \\ \hline
		\texttt{/api/admin/users/}, \texttt{/admin/roles/} & User account management, role assignment, and role permission management for authorized administrators. \\ \hline
		\texttt{/api/activity-logs/} & Read-only activity history for login, create, update, and delete events, available only to users with activity-log access. \\ \hline
	\end{tabularx}
\end{table}

Pagination is opt-in. If a page parameter is not sent, list endpoints can still return a plain array for compatibility with existing frontend logic. When pagination is requested, the response includes \texttt{count}, \texttt{next}, \texttt{previous}, \texttt{page}, \texttt{page\_size}, \texttt{total\_pages}, and \texttt{results}.

\section{Database Design Methodology}
The database was designed as a relational MySQL schema managed by Django migrations. Master data is separated from transaction data. For example, products, suppliers, customers, and categories are stored once and then referenced by purchases, sales, quotations, billing notes, payment batches, and credit notes.

The system also keeps snapshot fields inside transaction records. A sale item stores product name, SKU, quantity, price, and amount. A purchase stores supplier name and line item details. These snapshot values are important because old business documents should remain readable even if a product, supplier, or customer is edited later.

Table~\ref{tab:database-model-groups} groups the principal database models according to the business records they store.

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\caption{Primary Database Model Groups}
	\label{tab:database-model-groups}
	\begin{tabularx}{\textwidth}{|B{3.1cm}|Z|}
		\hline
		\textbf{Group} & \textbf{Models} \\ \hline
		Master data & \texttt{Category}, \texttt{Supplier}, \texttt{Customer}, \texttt{Product}, \texttt{ProductPicture}, \texttt{ProductUnitConversion}, \texttt{ProductSupplier} \\ \hline
		Purchasing & \texttt{Purchase}, \texttt{PurchaseItem}, \texttt{PurchaseDocument} \\ \hline
		Sales & \texttt{Sale}, \texttt{SaleItem}, \texttt{SaleItemAllocation}, \texttt{SaleDocument} \\ \hline
		Quotation & \texttt{Quotation}, \texttt{QuotationItem}, \texttt{QuotationItemSupplier} \\ \hline
		Finance & \texttt{BillingNote}, \texttt{BillingNoteLine}, \texttt{PaymentBatch}, \texttt{PaymentBatchLine}, \texttt{CreditNote}, \texttt{CreditNoteLine} \\ \hline
	\end{tabularx}
\end{table}

\section{Business Workflow Design}
The main workflow starts from master data. Products belong to categories and are used in purchase, sale, and quotation line items. Purchases bring stock into the system only when purchase items are received. Sales commit stock only when sale items are packed, shipped, or delivered. This is why the system does not treat product stock as a manually edited field.

Quotations are used before purchase or sales records are created. Billing notes are created from eligible sales for customer receivable tracking. Payment batches are created from eligible purchases for supplier payable tracking. Credit notes are created from eligible cancelled or returned sale lines. Dashboard summaries, AI Chat, and AI Report read from the operational records, while user access and activity logs support administration and audit review.

Figure~\ref{fig:business-workflow-design} shows how master data, transactions, stock movement, finance documents, reports, and access control are connected in the overall process.

\vspace{0.6\baselineskip}

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{
\begin{tikzpicture}[
	>=Stealth,
	font=\tiny,
	arrow/.style={->, thick, shorten >=2pt, shorten <=2pt, line cap=round, line join=round},
	base/.style={rectangle, rounded corners=2pt, draw=black!75, align=center, text width=2.5cm, minimum height=0.7cm},
	master/.style={base, thick, text width=3.75cm, fill=blue!8},
	quotation/.style={base, fill=blue!25},
	action/.style={base, fill=blue!12},
	decision/.style={diamond, draw=black!75, thick, align=center, aspect=2.25, text width=2.05cm, inner sep=1pt, fill=yellow!28},
	stock/.style={base, thick, fill=magenta!18},
	billing/.style={base, fill=green!25},
	payment/.style={base, fill=red!20},
	credit/.style={base, fill=red!12},
	report/.style={base, text width=2.6cm, fill=cyan!18},
	assistant/.style={base, text width=2.25cm, fill=violet!15},
	admin/.style={base, text width=2.65cm, fill=orange!16}
]
	\node[master] (master) at (0,0) {Master Data\\Categories / Products / Suppliers / Customers};
	\node[quotation] (quotation) at (0,-1.55) {Quotation};
	\node[decision] (check) at (0,-3.45) {Stock\\Sufficiency\\Check};
	\node[action] (purchase) at (-4.45,-5.45) {Purchase Order};
	\node[action] (sale) at (3.85,-5.45) {Sale Order};
	\node[action] (received) at (-4.45,-7.15) {Received Purchase Items};
	\node[stock] (available) at (-0.45,-7.15) {Available Stock};
	\node[action] (fulfilled) at (3.85,-7.15) {Packed / Shipped / Delivered Sale Items};
	\node[action] (eligiblepurchases) at (-6.7,-8.85) {Eligible Purchases};
	\node[payment] (payment) at (-6.7,-10.45) {Payment Batches};
	\node[billing] (billing) at (1.35,-8.85) {Billing Notes};
	\node[action] (cancelled) at (6.7,-8.85) {Cancelled / Returned Sale Items};
	\node[credit] (credit) at (6.7,-10.45) {Credit Notes};
	\node[report] (dashboard) at (0,-12.15) {Dashboard};
	\node[assistant] (chat) at (-1.7,-13.75) {AI Chat};
	\node[assistant] (aireport) at (1.7,-13.75) {AI Report};
	\node[admin] (access) at (6.7,-1.55) {User Access\\Roles / Permissions};
	\node[admin] (logs) at (6.7,-3.45) {Activity Logs};
	\coordinate (leftMasterLane) at (-6.05,0);
	\coordinate (rightMasterLane) at (4.6,0);
	\coordinate (rightReturnLane) at (8.25,0);
	\coordinate (dashE) at ([xshift=-1.0cm]dashboard.north);
	\coordinate (dashH) at ([xshift=-0.35cm]dashboard.north);
	\coordinate (dashI) at ([xshift=0.35cm]dashboard.north);
	\coordinate (dashK) at (dashboard.west);
	\coordinate (dashM) at (dashboard.east);
	\coordinate (purchaseFinanceLane) at (-6.7,-5.45);
	\coordinate (purchaseMasterDrop) at ([yshift=0.65cm]purchase.north);
	\coordinate (saleMasterDrop) at ([yshift=0.65cm]sale.north);
	\coordinate (cancelledSaleDrop) at ([yshift=0.65cm]cancelled.north);
	
	\draw[arrow] (master) -- (quotation);
	\draw[arrow] (master.west) -- (leftMasterLane |- master.west) |- (purchaseMasterDrop) -- (purchase.north);
	\draw[arrow] (master.east) -- (rightMasterLane |- master.east) |- (saleMasterDrop) -- (sale.north);
	\draw[arrow] (quotation) -- (check);
	\draw[arrow] (check.south east) -- node[pos=0.48, above, sloped]{Enough stock} (sale.north west);
	\draw[arrow] (check.south west) -- node[pos=0.5, above, sloped]{Short stock} (purchase.north east);
	\draw[arrow] (purchase) -- (received);
	\draw[arrow] (received) -- (available);
	\draw[arrow] (available) -- (sale);
	\draw[arrow] (sale) -- (fulfilled);
	\draw[arrow] (fulfilled) -- (billing);
	\draw[arrow] (purchase.west) -- (purchaseFinanceLane) -- (eligiblepurchases.north);
	\draw[arrow] (eligiblepurchases) -- (payment);
	\draw[arrow] (sale.east) -- (rightReturnLane |- sale.east) |- (cancelledSaleDrop) -- (cancelled.north);
	\draw[arrow] (cancelled) -- (credit);
	\draw[arrow] (received.south east) -- ++(0,-3.65) -| (dashE);
	\draw[arrow] (fulfilled.south) -- ++(0,-3.25) -| (dashH);
	\draw[arrow] (billing.south) -- ++(0,-2.1) -| (dashI);
	\draw[arrow] (payment.east) -- (dashK);
	\draw[arrow] (credit.east) -- (rightReturnLane |- credit.east) |- (rightReturnLane |- dashboard.east) -- (dashM);
	\draw[arrow] (dashboard) -- (chat);
	\draw[arrow] (dashboard) -- (aireport);
	\draw[arrow] (access) -- (logs);
\end{tikzpicture}
}
\vspace{1em}
\caption{Business Workflow Design}
\label{fig:business-workflow-design}
\end{figure}

\section{Validation and Security Design}
Validation is handled mainly by the backend. The frontend helps the user by showing form messages and previews, but direct API calls still need to be safe. The backend validates stock availability, active product usage, allocation rules, document eligibility, quotation validity dates, and finance document selection rules.

Security begins with the login process. A user enters an assigned username and password, and the backend verifies those credentials before issuing an access token and a refresh token. The frontend sends the access token with later API requests and uses the refresh token to obtain a replacement when the access token expires. After login, the authenticated profile endpoint returns the user's roles, permissions, and page-access flags so that the interface and protected backend actions apply the same access rules. Simple JWT provides the tokens, while Django groups and model permissions define what each role can do. In local development, authentication can be optional through \texttt{INVENTORY\_REQUIRE\_AUTH}; in an authenticated deployment, protected actions require both a valid login and the necessary permission.

\section{Testing Methodology}
Testing was designed to check both technical correctness and user understanding of the program flow. The automated tests focused on backend behavior because the backend owns the most important business rules, including stock calculation, stock validation, finance eligibility, payable synchronization, and AI data preparation. Manual user testing followed the main transaction process, and questionnaires recorded whether participants could understand the interface and complete the required tasks.

Table~\ref{tab:testing-summary} provides an overview of the areas checked before the individual testing methods are described.

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\caption{Testing Summary}
	\label{tab:testing-summary}
	\begin{tabularx}{\textwidth}{|B{3.2cm}|Z|}
		\hline
		\textbf{Test Area} & \textbf{What It Checks} \\ \hline
		Pagination and filtering & Paginated responses, page size limits, search, status filters, date filters, and product or partner filters. \\ \hline
		Product data & Product image/PDF upload, active/disabled behavior, lookup data, stock filter, average cost, margin visibility, and product history. \\ \hline
		Stock validation & Sales cannot commit more stock than available received purchase layers. \\ \hline
		Stock allocation & FIFO allocation, manual allocation, and stock layer remaining quantity are handled correctly. \\ \hline
		Reorder calculation & Fixed mock purchase and sale data is used to confirm available stock, average cost, lead time, daily demand, safety stock, reorder level, recommended restock, days until stockout, and stock value. \\ \hline
		Finance eligibility & Billing notes, payment batches, and credit notes only use eligible records. \\ \hline
		Financial synchronization & Purchase payable totals and payment batch line amounts stay consistent when purchases change. \\ \hline
		User access control & Backend tests confirm that regular users cannot open administrator user endpoints, authorized administrators can create users and assign roles, and model permissions control protected API access when authentication enforcement is enabled. \\ \hline
		Assistant scope & Supported questions return data-based answers, while unsupported questions are rejected as out of scope. \\ \hline
		AI report output & Supplier, customer, and product report contexts are checked against backend data, and generated report HTML is sanitized and rendered with deterministic chart rows. \\ \hline
\end{tabularx}
\end{table}

\subsection{Automated Backend Validation Method}
Automated backend validation was conducted with Django and Django REST Framework test cases. The tests used a temporary test database, created controlled records, called backend APIs or service functions, and compared the returned values with expected behavior. This method was used because backend rules must still be correct even if a user bypasses the frontend and calls the API directly.

The preserved black-box test history contains a selected 30-case workflow suite for presentation. That historical selection covered product validation, quotation behavior, purchase workflow, sales stock validation, finance eligibility, credit notes, payment batches, payable synchronization, and lookup behavior. It did not contain dedicated pagination or filtering cases. The final full backend suite contains 106 tests, including separate coverage for pagination, filtering, reorder calculation, AI Chat, AI Report, user access, permission enforcement, and activity logging.

Table~\ref{tab:automated-backend-test-method} describes how controlled data, test actions, and expected results were used for repeatable backend validation.

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\caption{Automated Backend Test Method}
	\label{tab:automated-backend-test-method}
	\begin{tabularx}{\textwidth}{|B{3.1cm}|Z|}
		\hline
		\textbf{Method Item} & \textbf{Description} \\ \hline
		Test data & Controlled products, suppliers, customers, purchases, sales, quotations, billing notes, payment batches, and credit notes were created inside the test database. \\ \hline
		Test action & The test cases called backend endpoints or backend service functions, similar to how the frontend sends requests during real use. \\ \hline
		Expected output & The returned API data, status codes, calculated values, and validation errors were compared with the expected business rules. \\ \hline
		Access-control checks & User-access tests checked that ordinary users cannot open administrator endpoints, administrators can create users and assign roles, permissions control protected API access, and activity-log access is restricted. \\ \hline
		Reason for method & Automated tests are repeatable and suitable for checking exact rules such as stock validation, eligibility, and financial synchronization. \\ \hline
	\end{tabularx}
\end{table}

\subsection{Calculation Validation Method}
The calculation validation focused on the reorder point and inventory dashboard calculation. The method used fixed mock data instead of live random data so that the expected output could be manually calculated and compared with the backend result.

The test created one product, received purchase stock, committed sales, pending sales, pending purchases, and lead-time samples. After the records were created, the dashboard stock report calculation was executed. The output was checked against expected values such as available stock, average unit cost, average daily demand, safety stock, reorder level, recommended restock, days until stockout, and stock value.

Table~\ref{tab:calculation-validation-method} summarizes the controlled inputs, comparison step, expected result, and reason for this calculation check.

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\caption{Calculation Validation Method}
	\label{tab:calculation-validation-method}
	\begin{tabularx}{\textwidth}{|B{3.1cm}|Z|}
		\hline
		\textbf{Method Item} & \textbf{Description} \\ \hline
		Input data & Fixed purchase and sale records were created for a known product so the expected stock calculation could be reproduced manually. \\ \hline
		Test action & The backend dashboard stock report logic was called using the controlled test records. \\ \hline
		Expected output & The backend should return the same reorder, restock, stock value, and stockout values as the manual calculation. \\ \hline
		Reason for method & Reorder point output affects purchasing decisions, so the formula must be validated using traceable input data. \\ \hline
	\end{tabularx}
\end{table}

\subsection{AI Chat Validation Method}
The AI Chat check was designed to answer two simple questions: whether the assistant uses the correct system records and whether it stays within its read-only purpose. Known sample records were prepared, and the same questions were asked so that names, totals, stock values, and document links could be compared with expected answers.

The program was checked in two operating conditions. Without an \mbox{OpenAI} API key, it should still answer supported questions with a limited summary prepared by the system. With an API key, it may improve the wording and add a conclusion and highlights, but the facts must remain the same. Unsupported requests should receive a clear out-of-scope message and a list of the stock, partner, finance, or document questions that the assistant can answer.

Table~\ref{tab:ai-chat-validation-method} summarizes the sample information, questions, expected behavior, and purpose of this check.

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\caption{AI Chat Validation Method}
	\label{tab:ai-chat-validation-method}
	\begin{tabularx}{\textwidth}{|B{3.1cm}|Z|}
		\hline
		\textbf{Check} & \textbf{Description} \\ \hline
		Sample information & Known customer, sale, stock, billing, and payment records were prepared so their values could be checked directly. \\ \hline
		Questions asked & The assistant received fixed questions about customer activity, net position, low stock, and a request outside its supported purpose. \\ \hline
		Expected behavior & Supported questions should use the prepared records. The no-key condition should return a limited local summary, the configured-key condition should add an explanation, conclusion, and highlights, and unsupported questions should be declined. \\ \hline
		Purpose & The check confirms that AI Chat helps users read existing information without inventing values or changing records. \\ \hline
	\end{tabularx}
\end{table}

\subsection{AI Report Validation Method}
The AI Report check examined whether a report showed the correct information for the supplier, customer, or product selected by the user. A small set of known records was prepared, a report was generated, and its names, transaction references, totals, charts, and tables were compared with those records. The print function and page structure were also checked.

The program was checked both with and without AI assistance. Without an \mbox{OpenAI} API key, the report should still contain the system-prepared facts and a local written summary. With an API key, AI may add written analysis, but it must not replace the verified totals, records, or chart data. The check also confirmed that unsafe webpage content, such as scripts, was removed before the report was displayed.

Table~\ref{tab:ai-report-validation-method} summarizes the information used, the report-generation step, the expected output, and the reason for the check.

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\caption{AI Report Validation Method}
	\label{tab:ai-report-validation-method}
	\begin{tabularx}{\textwidth}{|B{3.1cm}|Z|}
		\hline
		\textbf{Check} & \textbf{Description} \\ \hline
		Sample information & A known supplier, customer, or product was selected together with related transactions, finance records, and stock information. \\ \hline
		Report generation & The program created the report for the selected record and prepared it as a printable webpage. \\ \hline
		Expected behavior & The report should show the correct record names, totals, references, charts, tables, and print control. Any unsafe webpage content should be removed. \\ \hline
		Purpose & The check confirms that optional AI writing does not change the verified business values used in the report. \\ \hline
	\end{tabularx}
\end{table}

\subsection{Manual Scenario Test Method}
Manual scenario testing was conducted to check whether users could complete the main transaction process in the system. The scenario was divided into parts that followed the intended sequence: quotation, purchase creation, purchase receiving, supplier payment, sale checking, status update, credit note, and billing note.

During the test, each participant followed the scenario steps and recorded the time and score for each part. Tester names were removed from the stored result file for privacy. This method was used because automated tests can prove that rules work, but they cannot show whether users can understand the screen flow and find the required actions.

Table~\ref{tab:manual-scenario-test-method} describes the prepared tasks, the participant action, the expected outcome, and the reason for manual testing.

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\caption{Manual Scenario Test Method}
	\label{tab:manual-scenario-test-method}
	\begin{tabularx}{\textwidth}{|B{3.1cm}|Z|}
		\hline
		\textbf{Method Item} & \textbf{Description} \\ \hline
		Input data & Testers used the prepared system data and followed scenario tasks covering the main transaction and finance workflow. \\ \hline
		Test action & Testers completed each scenario part while completion time, task score, and comments were collected. \\ \hline
		Expected output & Users should be able to complete the main workflow and identify where the system is easy or difficult to use. \\ \hline
		Reason for method & Manual testing checks practical usability and workflow clarity from the user's point of view. \\ \hline
	\end{tabularx}
\end{table}

\subsection{Questionnaire Method}
After the scenario test, participants answered a questionnaire using a five-point rating scale, where a higher value represented a more positive response. The questions covered ease of use, clarity of the transaction steps, usefulness of the information shown, and overall confidence in using the system.

The ratings were averaged to show the general response to each part of the system. Written comments were read and grouped by common ideas mentioned by more than one participant, such as difficulty finding buttons or understanding a status. The questionnaire supported the technical tests by showing how people understood the program after using it; it was not used as a replacement for functional validation.

Table~\ref{tab:questionnaire-method} explains the information collected, how it was summarized, the expected result, and the purpose of the questionnaire.

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\caption{Questionnaire Method}
	\label{tab:questionnaire-method}
	\begin{tabularx}{\textwidth}{|B{3.1cm}|Z|}
		\hline
		\textbf{Step} & \textbf{Description} \\ \hline
		Information collected & Participants provided ratings and optional written comments after completing the scenario tasks. \\ \hline
		Summary method & Ratings were averaged, and comments with similar meaning were grouped into common positive points and improvement areas. \\ \hline
		Expected result & The questionnaire should show how understandable and useful the system appeared to participants and identify interface areas that need improvement. \\ \hline
		Purpose & The questionnaire adds user feedback that cannot be measured by automated program tests alone. \\ \hline
	\end{tabularx}
\end{table}

\section{Tools and Technologies Used}
The project used separate tools for frontend development, backend development, database storage, authentication, AI integration, styling, and testing. These tools were selected to support a full-stack web application with server-side business rules and a React-based user interface.

Table~\ref{tab:tools-technologies} lists each main tool and the role it serves in the completed system.

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\caption{Tools and Technologies}
	\label{tab:tools-technologies}
	\begin{tabularx}{\textwidth}{|B{2.8cm}|B{3.2cm}|Z|}
		\hline
		\textbf{Category} & \textbf{Tool or Technology} & \textbf{Use in This Project} \\ \hline
		Frontend framework & React 18 & Builds the web application screens and reusable UI components. \\ \hline
		Frontend build tool & Vite & Runs the local development server and production build process. \\ \hline
		Backend framework & Django 5 & Provides project structure, ORM, migrations, settings, and backend application logic. \\ \hline
		API framework & Django REST Framework & Provides serializers, viewsets, API views, JSON responses, parsers, and API tests. \\ \hline
		Database & MySQL & Stores relational inventory, transaction, and finance data. \\ \hline
		Authentication and access control & Simple JWT and Django permissions & Provides access and refresh token authentication, authenticated profiles, user-role management, permission checks, and activity-log access control. \\ \hline
		AI integration & OpenAI API with local fallback & Uses structured model output for read-only chat explanations, conclusions, and highlights, and for AI report analysis when configured; backend-prepared local output remains available after missing keys or API failures. \\ \hline
		Styling & CSS files by domain & Provides the compact inventory system interface. \\ \hline
		Testing & Django TestCase and DRF APITestCase & Validates backend business rules and API behavior. \\ \hline
	\end{tabularx}
\end{table}
