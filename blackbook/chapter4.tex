\chapter{RESULTS}

This chapter explains the result of the implemented Inventory Management System. The result is based on the current codebase and implemented web-application pages. The calculation basis is defined in Chapter 2 and the development method in Chapter 3, so this chapter focuses on demonstrated workflows, evidence, and evaluation results.

\section{System Overview}
The final system is a full-stack web application for an SME trading business. It supports the flow from product setup to purchasing, selling, billing, supplier payment, credit note adjustment, inventory checking, dashboard reporting, and limited AI-assisted questions.

The main result is that the system connects business documents together instead of keeping them as separate records. A product can appear in purchases, sales, quotations, inventory reports, and product history. A sale can later become part of a billing note or a credit note. A purchase can later become part of a payment batch. This connection makes the system more useful than a simple product list.

\section{Implemented Modules}
All requirement areas mapped in Tables~\ref{tab:requirement-page-map} and~\ref{tab:record-support-page-map} were implemented. The completed workspace includes Dashboard, Inventory, AI Chat, AI Report, Purchases, Payment Batches, Quotation, Sales, Billing Notes, Credit Notes, Products, Categories, Supplier, Customer, User Access, Activity Log, and Settings. The following section presents page-level evidence without repeating the full requirement map.

\section{System Implementation}
This section combines the page-development explanation with selected screenshots from the finished application. Each figure is described and referenced first, with the corresponding screenshot placed directly underneath. This consistent order connects every screenshot to the text that explains it.

Figure~\ref{fig:dashboard-page} presents the dashboard page, which is the main post-login business overview. It receives backend dashboard data and shows stock health, reorder planning, stock cycling, delivery pipeline, purchase planning, popular products, finance, cashflow, and order coverage. It is connected to other pages, so a low-stock product can lead the user to the inventory or product page, and an order issue can lead to the purchase or sales page.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.95\textwidth,height=0.62\textheight,keepaspectratio]{figure-4-1-dashboard.png}
	\vspace{10pt}
	\caption{Dashboard Page}
	\label{fig:dashboard-page}
\end{figure}

Figure~\ref{fig:inventory-page} presents the inventory page used for daily stock checking and reorder review. It uses backend stock report rows instead of calculating stock from full transaction lists in the browser. The interface includes an inventory control board, searchable directory, filters for health status, category, supplier, value, and reorder need, product detail views, and a quick purchase-order drawer.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.95\textwidth,height=0.62\textheight,keepaspectratio]{figure-4-2-inventory.png}
	\vspace{10pt}
	\caption{Inventory Page}
	\label{fig:inventory-page}
\end{figure}

Figure~\ref{fig:ai-chat-page} presents the AI Chat page used for read-only questions about stock, partners, receivables, payables, and document references. The program first prepares the factual answer from current records. Without an OpenAI API key, the page displays a limited local summary. With a configured key, AI improves the explanation and adds a conclusion and question-specific highlights without changing the prepared facts. The assistant does not create, edit, approve, cancel, or delete records.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.95\textwidth,height=0.62\textheight,keepaspectratio]{figure-4-3-ai-chat.png}
	\vspace{10pt}
	\caption{AI Chat Assistant Page}
	\label{fig:ai-chat-page}
\end{figure}

Figure~\ref{fig:ai-report-page} presents the AI Report page used to create printable supplier, customer, or product reports. The user chooses the report type and record and may narrow the reporting dates. The program collects related transactions, finance records, stock values, charts, and tables. Without an OpenAI API key, it produces a local report; with a configured key, AI adds written analysis to the same verified information. The final report opens as a printable webpage.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.95\textwidth,height=0.62\textheight,keepaspectratio]{figure-4-4-ai-report.png}
	\vspace{10pt}
	\caption{AI Report Generation Page}
	\label{fig:ai-report-page}
\end{figure}

Figure~\ref{fig:product-management-form} presents the product form used to maintain item master data, including product identifiers, names, category, units and conversions, reorder settings, image or PDF attachments, supplier links, and cost or margin information. Products connected to transactions can be disabled instead of deleted so that historical purchases, sales, and quotations remain readable.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.95\textwidth,height=0.62\textheight,keepaspectratio]{figure-4-5-product-form.png}
	\vspace{10pt}
	\caption{Product Management Form}
	\label{fig:product-management-form}
\end{figure}

The other master-data pages support the same business foundation. The categories page organizes products in a parent-child tree and shows the full hierarchy in one view. The supplier page stores company profile, contact details, taxpayer ID, branches, shipping addresses, remarks, payment terms, and procurement contact details. The customer page stores company profile, contact details, branches, shipping addresses, remarks, and billing or payment term information.

Figure~\ref{fig:purchase-workflow} presents the purchase page used to enter and review supplier purchase orders. It records the supplier, items, attached documents, overall purchase status, receiving status, taxes, discounts, and delivery dates. Only an item marked as received becomes available stock.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.95\textwidth,height=0.62\textheight,keepaspectratio]{figure-4-6-purchase-workflow.png}
	\vspace{10pt}
	\caption{Purchase Workflow}
	\label{fig:purchase-workflow}
\end{figure}

Figure~\ref{fig:sales-workflow} presents the sales page used to prepare and track customer orders. It records customer details, the customer purchase-order reference, items, documents, tax totals, discounts, and delivery statuses. Before a sale can reserve or issue stock, the available quantity is checked and the sold quantity is assigned to received purchase batches.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.95\textwidth,height=0.62\textheight,keepaspectratio]{figure-4-7-sales-workflow.png}
	\vspace{10pt}
	\caption{Sales Workflow}
	\label{fig:sales-workflow}
\end{figure}

Figure~\ref{fig:quotation-workflow} presents the quotation page used before a quotation is converted into a purchase or sale. It records requested items, supplier cost options, selling prices, gross-margin review, validity, shipping date, payment terms, VAT, and total values. Quotation lines are stored in the relational \textit{QuotationItem} and \textit{QuotationItemSupplier} tables.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.95\textwidth,height=0.62\textheight,keepaspectratio]{figure-4-8-quotation-workflow.png}
	\vspace{10pt}
	\caption{Quotation Workflow}
	\label{fig:quotation-workflow}
\end{figure}

Figure~\ref{fig:billing-note-workflow} presents the billing note page used to group eligible customer sales into a receivable document. It tracks the included sales, outstanding or received status, expected and actual payment dates, bank references, and related credit notes.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.95\textwidth,height=0.62\textheight,keepaspectratio]{figure-4-9-billing-note-workflow.png}
	\vspace{10pt}
	\caption{Billing Note Workflow}
	\label{fig:billing-note-workflow}
\end{figure}

Figure~\ref{fig:credit-notes-page} presents the Credit Notes page used to record adjustments after a sale is cancelled or returned. The backend provides eligible sale lines, and the credit note stores customer information, sale reference, optional billing note link, line items, and total credited amount. This keeps the credit document connected to the original sale item while preserving a readable adjustment record.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.95\textwidth,height=0.62\textheight,keepaspectratio]{figures/chap4/credit notes.png}
	\vspace{10pt}
	\caption{Credit Notes Page}
	\label{fig:credit-notes-page}
\end{figure}

Figure~\ref{fig:payment-batch-workflow} presents the payment batch page used to group eligible supplier purchases for payment. It stores the supplier, planned and actual payment dates, bank reference, total amount, and paid status for each included purchase.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.95\textwidth,height=0.62\textheight,keepaspectratio]{figure-4-10-payment-batch-workflow.png}
	\vspace{10pt}
	\caption{Payment Batch Workflow}
	\label{fig:payment-batch-workflow}
\end{figure}

Figure~\ref{fig:user-access-page} presents the User Access page used by an administrator to manage staff accounts and roles. The login and access process uses password verification, access and refresh tokens, authenticated profiles, and permission checks to control protected pages and actions.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.95\textwidth,height=0.62\textheight,keepaspectratio]{figures/chap4/user access.png}
	\vspace{10pt}
	\caption{User Access Management Page}
	\label{fig:user-access-page}
\end{figure}

Figure~\ref{fig:activity-log-page} presents the Activity Log page used to review important events such as login, record creation, update, and deletion. Access to this page depends on the user's assigned role and permissions.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.95\textwidth,height=0.62\textheight,keepaspectratio]{figure-4-12-activity-log.png}
	\vspace{10pt}
	\caption{Activity Log Page}
	\label{fig:activity-log-page}
\end{figure}

Figure~\ref{fig:settings-page} presents the Settings page used to switch the interface between English and Thai. The language choice changes user-facing labels and guidance while preserving the same records and backend business rules.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.95\textwidth,height=0.62\textheight,keepaspectratio]{figures/chap4/setting.png}
	\vspace{10pt}
	\caption{Settings Page}
	\label{fig:settings-page}
\end{figure}

\section{Results by Workflow}
The workflow results are connected directly to the relational database design. Master records such as products, suppliers, customers, and categories provide the reusable business entities. Transaction records then reference those master records while keeping snapshot fields, so historical documents remain readable even if master data changes later. Line-item tables store the actual product quantities, prices, statuses, and totals used by stock, finance, dashboard, and report calculations.

Table~\ref{tab:workflow-database-entity-map} connects each visible workflow area to the main database records that produce its result. It shows that the pages are not isolated: master data supports transactions, transaction lines produce stock movement, and finance or reporting functions read those same records.

\begin{table}[!htbp]
	\centering
	\BlackbookTableSetup
	\renewcommand{\arraystretch}{1.3}
	\caption{Workflow Results and Related Database Entities}
	\label{tab:workflow-database-entity-map}
	\begin{tabularx}{\textwidth}{|B{3cm}|B{4.7cm}|Z|}
		\hline
		\textbf{Workflow Area} & \textbf{Main Database Entities} & \textbf{Relationship to the Result} \\ \hline
		Master data & \texttt{Product}, \texttt{Category}, \texttt{Supplier}, \texttt{Customer}, \texttt{ProductPicture}, \texttt{ProductUnitConversion}, \texttt{ProductSupplier} & These tables store reusable product and partner records. Transactions reference them through foreign keys while also keeping snapshot names, SKUs, and terms for historical documents. \\ \hline
		Purchasing and stock & \texttt{Purchase}, \texttt{PurchaseItem}, \texttt{PurchaseDocument}, \texttt{Product}, \texttt{Supplier} & Purchase headers link to suppliers, and purchase items link to products. Received purchase items are the inbound stock source used by inventory and dashboard calculations. \\ \hline
		Sales & \texttt{Sale}, \texttt{SaleItem}, \texttt{SaleItemAllocation}, \texttt{PurchaseItem}, \texttt{Customer}, \texttt{Product} & Sale headers link to customers, sale items link to products, and allocation rows connect sold quantities back to received purchase layers for traceability. \\ \hline
		Quotations & \texttt{Quotation}, \texttt{QuotationItem}, \texttt{QuotationItemSupplier}, \texttt{Customer}, \texttt{Supplier}, \texttt{Product} & Quotation headers store customer and supplier context, quotation items store requested products, and supplier option rows store alternative costs for each line. \\ \hline
		Finance & \texttt{BillingNote}, \texttt{BillingNoteLine}, \texttt{PaymentBatch}, \texttt{PaymentBatchLine}, \texttt{CreditNote}, \texttt{CreditNoteLine}, \texttt{Sale}, \texttt{Purchase} & Finance documents group eligible sales or purchases through line tables, while credit notes reference the original sale so adjustments stay tied to the source transaction. \\ \hline
		Dashboard and inventory & \texttt{Product}, \texttt{PurchaseItem}, \texttt{SaleItem}, \texttt{ProductSupplier}, \texttt{BillingNote}, \texttt{PaymentBatch} & Dashboard and inventory values are calculated from existing operational tables instead of storing a separate mutable stock total. \\ \hline
		AI assistant and AI report & \texttt{Product}, \texttt{Supplier}, \texttt{Customer}, \texttt{Purchase}, \texttt{Sale}, \texttt{Quotation}, \texttt{BillingNote}, \texttt{PaymentBatch}, \texttt{CreditNote} & Assistant answers and printable reports use backend-prepared records from the same database entities that support the normal workflows. \\ \hline
		User access and activity log & \texttt{User}, \texttt{Group}, \texttt{Permission}, \texttt{ActivityLog} & Django authentication tables store users, roles, and permissions, while the activity log records important system actions for review. \\ \hline
	\end{tabularx}
\end{table}

The key workflow result is that each module uses the same normalized database instead of keeping isolated screen-only data. Master data supports transactions, transaction line items drive stock and financial values, and finance documents reference eligible source records. This makes the system traceable from product setup through purchasing, sales, billing, payment, reporting, and audit review.

\begin{enumerate}
	\item Master data provides the reusable product, category, supplier, and customer records needed by every transaction workflow.
	\item Purchasing and sales line items are the main source for stock movement. Received purchase items add stock, while committed sale items allocate stock from received purchase layers.
	\item Quotations, billing notes, payment batches, and credit notes keep business documents connected through database references, reducing duplicate or unrelated finance records.
	\item Dashboard, inventory, AI chat, and AI report outputs use backend-prepared data from the same transaction tables, so summary information stays consistent with the stored records.
	\item User access and activity logs add control and traceability by connecting staff accounts, roles, permissions, and important system events.
\end{enumerate}

\section{Feature Evidence Summary}
Table~\ref{tab:feature-evidence-summary} summarizes the implemented evidence used to confirm the main technical features. Each row states the observable behavior that demonstrates the feature in the completed system.

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\caption{Evidence of Implemented Features}
	\label{tab:feature-evidence-summary}
	\begin{tabularx}{\textwidth}{|B{3.4cm}|Z|}
		\hline
		\textbf{Feature} & \textbf{Evidence in the Implemented System} \\ \hline
		REST API communication & The frontend API client calls endpoints for dashboard, lookups, products, purchases, sales, quotations, billing notes, payment batches, credit notes, authentication, and chat. \\ \hline
		JWT authentication and user access & The backend provides login, refresh, profile, user-management, role-management, and activity-log endpoints. The frontend stores and refreshes access tokens, shows permission-based navigation, and provides a user-access page for authorized administrators. \\ \hline
		Backend stock validation & The sale workflow calls backend validation before stock-deducting sale statuses are committed. \\ \hline
		FIFO and manual allocation & The backend creates sale allocations from received purchase layers and validates manual layer selections. \\ \hline
		Finance eligibility & Backend endpoints return only eligible sales for billing notes, eligible purchases for payment batches, and eligible sale lines for credit notes. \\ \hline
		Normalized quotation items & Quotation lines and supplier options are stored in relational tables while the API still returns a simple \texttt{items} array to the frontend. \\ \hline
		Backend dashboard summaries & The dashboard and inventory pages use backend-prepared summary data instead of calculating all operational metrics from full transaction lists in the browser. \\ \hline
		Assistant scope control & The chat service answers only supported read-only workflows and rejects unsupported requests as outside scope. \\ \hline
		AI report generation & The AI report endpoint collects transaction data for a selected record, calculates metrics and charts, and returns a printable HTML report with optional AI-written analysis. \\ \hline
	\end{tabularx}
\end{table}

The evidence shows that important rules are enforced by shared backend services and database records rather than by individual screens alone. This is especially important for stock allocation, finance eligibility, access control, and AI output because these functions must remain consistent regardless of which page requests the information.

\section{Testing and Evaluation Results}
The system was evaluated using three types of evidence: automated backend validation, manual scenario testing, and user questionnaire results. Automated tests were used for business rules that must be exact, such as stock calculation, eligibility, and AI output validation. Scenario testing and questionnaires were used to evaluate how users performed the main workflow and how they perceived the system.

\subsection{Automated Backend Validation}
The final full backend suite contained 106 automated tests, and all 106 passed. This suite includes pagination, filtering, product data, transaction validation, stock allocation, finance eligibility, calculation, AI Chat, AI Report, user access, permission enforcement, and activity logging. For presentation continuity, the Blackbook also preserves a historical 30-case black-box aligned workflow selection; all 30 selected cases passed. The smaller recorded 5-test AI/calculation run and 6-test access/activity run are therefore treated as earlier focused evidence rather than the current total.

Table~\ref{tab:automated-validation-result-summary} presents the test groups, pass results, and the main behavior confirmed by each group.

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\caption{Automated Validation Result Summary}
	\label{tab:automated-validation-result-summary}
	\begin{tabularx}{\textwidth}{|B{3.5cm}|D{2cm}|Z|}
		\hline
		\textbf{Test Group} & \textbf{Result} & \textbf{Main Evidence} \\ \hline
		Full current backend suite & 106/106 passed & The final Django inventory test suite passed across API, business-rule, calculation, AI, access-control, pagination, and filtering coverage. \\ \hline
		Selected backend workflow tests & 30/30 passed & Product rules, quotation behavior, purchase workflow, sale stock validation, finance eligibility, credit notes, payment batches, and payable synchronization passed. \\ \hline
		Reorder point calculation & Passed & The backend returned reorder level 48, recommended restock 18, days until stockout 15, and stock value 300 from fixed mock data. \\ \hline
		AI Chat validation & Passed & Local chat returned deterministic backend summaries; configured model responses retained the same presentation and added validated answer, conclusion, and highlights fields. \\ \hline
		AI Report validation & Passed & Supplier, customer, and product reports used backend context, returned printable HTML, replaced poor model chart HTML with backend chart rows, and sanitized unsafe HTML. \\ \hline
		User access and activity log validation & 6/6 passed & Regular users were blocked from administrator endpoints, administrators could create users and assign roles, model permissions controlled protected access, and activity-log access was restricted. \\ \hline
	\end{tabularx}
\end{table}

\subsection{Calculation Validation}
The calculation validation focused on the reorder point and recommended restock calculation because this is one of the most important inventory decisions in the system. The test did not use random data. Instead, it created fixed purchase and sale records in a temporary Django test database so that the expected result could be calculated manually.

The test first created one product, two received purchase records, one pending purchase record, two committed sales, and one pending sale. Then the test called the dashboard API, which runs the same backend stock report calculation used by the real dashboard and inventory page. The backend output was compared with the manually calculated expected values.

Table~\ref{tab:calculation-validation-input} lists the controlled input values and explains why each value was included. Table~\ref{tab:calculation-validation-output} then compares the manually expected result with the value returned by the program. The tested calculation follows the reorder components and restock definitions in Equations~\ref{eq:reorder-components} and~\ref{eq:restock-and-value}.

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\caption{Calculation Validation Input}
	\label{tab:calculation-validation-input}
	\begin{tabularx}{\textwidth}{|B{3.2cm}|B{2.6cm}|Z|}
		\hline
		\textbf{Input Item} & \textbf{Value} & \textbf{Reason for Test} \\ \hline
		Product & TEST-PEN & A single controlled product makes the stock row easy to identify. \\ \hline
		Received purchase units & 100 pcs & Used as the stock added into the system. \\ \hline
		Received purchase value & 500 & Used to calculate average unit cost. \\ \hline
		Committed sale units & 40 pcs & Used as stock already allocated to fulfilled sales. \\ \hline
		Pending sale units & 20 pcs & Used as future demand that should affect restock recommendation. \\ \hline
		Pending purchase units & 10 pcs & Used as incoming stock that should reduce restock recommendation. \\ \hline
		Lead time samples & 4 days and 6 days & Used to calculate average lead time of 5 days. \\ \hline
		Sales history & 40 pcs over 10 days & Used to calculate average daily demand of 4 pcs per day. \\ \hline
	\end{tabularx}
\end{table}

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\caption{Calculation Validation Output}
	\label{tab:calculation-validation-output}
	\begin{tabularx}{\textwidth}{|B{3.2cm}|B{2.4cm}|B{2.4cm}|Z|}
		\hline
		\textbf{Output Item} & \textbf{Expected Value} & \textbf{Backend Value} & \textbf{Result} \\ \hline
		Available stock & 60 & 60 & Passed \\ \hline
		Average unit cost & 5 & 5 & Passed \\ \hline
		Average lead time & 5 days & 5 days & Passed \\ \hline
		Average daily demand & 4 pcs/day & 4 pcs/day & Passed \\ \hline
		Safety stock & 28 & 28 & Passed \\ \hline
		Reorder level & 48 & 48 & Passed \\ \hline
		Recommended restock & 18 & 18 & Passed \\ \hline
		Days until stockout & 15 days & 15 days & Passed \\ \hline
		Stock value & 300 & 300 & Passed \\ \hline
	\end{tabularx}
\end{table}

As shown in Table~\ref{tab:calculation-validation-output}, every calculated output matched the manually expected value. The result confirms that the same controlled inputs produce available stock of 60 units, a reorder level of 48 units, a recommended restock quantity of 18 units, and a stock value of 300.

\subsection{AI Chat Validation}
The AI Chat validation checked whether the chat page answers from backend data instead of inventing business values. The test was performed by calling the backend chat function with fixed questions and known data. The important output fields were the answer text, presentation title, selected record references, model marker, conclusion, and highlights.

Without an API key, AI Chat returns a limited local summary prepared from the selected records. With a configured key, AI improves the wording and adds a conclusion and highlights while using the same factual information. If the AI request fails, the local summary remains available. Requests outside the supported read-only topics receive an out-of-scope response instead of an invented answer.

Table~\ref{tab:ai-chat-validation-input-output} shows the sample questions, the known information used for comparison, the expected answer, and the result for both local and AI-assisted conditions.

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\caption{AI Chat Validation Input and Output}
	\label{tab:ai-chat-validation-input-output}
	\begin{tabularx}{\textwidth}{|B{2.8cm}|B{2.8cm}|Z|D{1.5cm}|}
		\hline
		\textbf{Test Question} & \textbf{Input Data Used} & \textbf{Expected Output Checked} & \textbf{Result} \\ \hline
		Customer activity summary & Customer with one known sale worth 275 & Chat title shows the selected customer, sales count is 1, sales total is 275, and the sale record target points to the created sale. & Passed \\ \hline
		Net position question & Existing billing note and payment batch data & Answer includes backend AR, AP, and net position values with presentation title ``Net position''. & Passed \\ \hline
		Low stock question & Product stock report rows from backend & Answer lists low-stock products and restock priorities from backend stock calculations. & Passed \\ \hline
		Unsupported quotation summary & A broad request outside the supported assistant scope & Assistant returns ``Outside current assistant scope'' and does not invent a quotation report. & Passed \\ \hline
		Configured key condition & API key enabled with a mocked structured model response & Chat returns the configured model marker, model wording, conclusion, and highlights while retaining the backend presentation and record targets. & Passed \\ \hline
	\end{tabularx}
\end{table}

All checks in Table~\ref{tab:ai-chat-validation-input-output} passed. Customer totals, net position, and low-stock information matched the prepared records; the unsupported request was declined; and the configured-key condition added a conclusion and highlights without changing the verified record references.

\subsection{AI Report Validation}
The AI Report validation checked whether supplier, customer, and product reports are generated from the correct selected records. The test also checked that the final report is printable HTML, that chart rows come from backend data, and that unsafe model HTML is removed.

The report test used a controlled supplier and purchase record. The program prepared the supplier name, purchase reference, purchase amount, metrics, chart rows, and tables. For the configured-key condition, a fixed AI response was used so that the same expected result could be checked every time. This controlled approach keeps automated tests repeatable.

Table~\ref{tab:ai-report-validation-input} lists the known report inputs. Table~\ref{tab:ai-report-validation-output} lists the expected document, chart, content-safety, and current-data checks performed on the generated report.

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\caption{AI Report Validation Input}
	\label{tab:ai-report-validation-input}
	\begin{tabularx}{\textwidth}{|B{3.1cm}|B{2.7cm}|Z|}
		\hline
		\textbf{Input Item} & \textbf{Value} & \textbf{Purpose} \\ \hline
		Report scope & Supplier report & Confirms that the selected report type controls which records are collected. \\ \hline
		Selected supplier & Configured Key Supplier & Confirms that the report context uses the selected supplier. \\ \hline
		Product & Configured Key Product & Confirms that line-item data is included in the report context. \\ \hline
		Purchase reference & PO-AI-KEY-001 & Confirms that the report includes the expected transaction record. \\ \hline
		Purchase amount & THB 450.00 & Confirms that report totals and table values match backend data. \\ \hline
		Model output & Simulated HTML body & Confirms how the system wraps model-style output into a final report document. \\ \hline
	\end{tabularx}
\end{table}

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\caption{AI Report Validation Output}
	\label{tab:ai-report-validation-output}
	\begin{tabularx}{\textwidth}{|B{3.1cm}|Z|D{1.5cm}|}
		\hline
		\textbf{Output Checked} & \textbf{Expected Result} & \textbf{Result} \\ \hline
		Report context & Contains Configured Key Supplier, PO-AI-KEY-001, and THB 450.00. & Passed \\ \hline
		Printable document & Final HTML contains document structure and a print button. & Passed \\ \hline
		AI model body handling & Simulated model summary remains in the final report after wrapping. & Passed \\ \hline
		Chart rendering & Poor model chart labels are replaced with backend chart row labels. & Passed \\ \hline
		HTML safety & Script tags, unsafe event handlers, iframes, and JavaScript links are removed. & Passed \\ \hline
		Current-data report check & Supplier, customer, and product reports generated from current database records and contained expected references and totals. & Passed \\ \hline
	\end{tabularx}
\end{table}

All checks in Table~\ref{tab:ai-report-validation-output} passed. The report contained the expected supplier, purchase reference, and amount; it remained printable; its charts used prepared data; and unsafe webpage elements were removed. These results show that optional AI writing did not replace the verified records and totals.

\subsection{Manual Scenario Test}
The manual scenario test asked users to complete the main process from quotation to purchase, purchase receiving, payment batch, sale, credit note, and billing note. Ten testers finished the scenario test.

Table~\ref{tab:scenario-test-participant-summary} summarizes the participant group and scenario coverage. Table~\ref{tab:scenario-test-average-result} then reports the average completion time, score, and interpretation for each part.

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\caption{Scenario Test Participant Summary}
	\label{tab:scenario-test-participant-summary}
	\begin{tabularx}{\textwidth}{|B{3.5cm}|Z|}
		\hline
		\textbf{Item} & \textbf{Result} \\ \hline
		Number of participants & 10 testers \\ \hline
		Age range & 19 to 68 years old \\ \hline
		Average age & 43.5 years old \\ \hline
		Roles represented & SME-related users and general evaluators, including owner, manager, purchasing/sales staff, executives, and students. \\ \hline
		Scenario parts & Part A to Part H, covering quotation, purchase, receiving, payment, sale, credit note, and billing note workflow. \\ \hline
	\end{tabularx}
\end{table}

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\caption{Average Scenario Test Time and Score}
	\label{tab:scenario-test-average-result}
	\begin{tabularx}{\textwidth}{|D{1.5cm}|B{2.2cm}|B{2cm}|Z|}
		\hline
		\textbf{Part} & \textbf{Average Time} & \textbf{Average Score} & \textbf{Interpretation} \\ \hline
		Part A & 151.9 seconds & 9.0/10 & Initial quotation workflow took the longest because it starts the process and requires more data entry. \\ \hline
		Part B & 71.8 seconds & 8.9/10 & Purchase creation from quotation was generally successful, but some users needed time to understand the flow. \\ \hline
		Part C & 29.7 seconds & 9.5/10 & Receiving purchase items was one of the faster and clearer tasks. \\ \hline
		Part D & 65.1 seconds & 9.5/10 & Payment-related workflow received high scores. \\ \hline
		Part E & 40.4 seconds & 9.2/10 & Sale-related checking was successful, although some users needed to read more information. \\ \hline
		Part F & 43.1 seconds & 8.9/10 & Status-related actions worked, but comments showed that status buttons should be clearer. \\ \hline
		Part G & 7.2 seconds & 9.3/10 & The shortest part, showing that the required action was quick once found. \\ \hline
		Part H & 49.1 seconds & 9.4/10 & Final billing or finance-related workflow received high scores. \\ \hline
	\end{tabularx}
\end{table}

The average scenario score across all parts was 9.21 out of 10. The lower relative scores in Part B and Part F match written comments about quotation-to-purchase flow, sale form details, and status-button clarity.

\subsection{Questionnaire Result}
After the scenario test, testers answered a 15-item questionnaire using a 1 to 5 rating scale. The average questionnaire score across all testers was 4.58 out of 5.

Table~\ref{tab:questionnaire-result-summary} summarizes the response count, scale, average score, and overall interpretation of the questionnaire.

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\caption{Questionnaire Result Summary}
	\label{tab:questionnaire-result-summary}
	\begin{tabularx}{\textwidth}{|B{3.5cm}|Z|}
		\hline
		\textbf{Evaluation Item} & \textbf{Result} \\ \hline
		Number of questionnaire responses & 10 responses \\ \hline
		Questionnaire items & 15 items \\ \hline
		Rating scale & 1 to 5 \\ \hline
		Average questionnaire score & 4.58/5 \\ \hline
		General result & Users gave mostly high scores, showing that the system was understandable and usable after completing the scenario. \\ \hline
	\end{tabularx}
\end{table}

\subsection{User Feedback Summary}
The user comments showed several repeated themes. Positive feedback mentioned that purchase order status was understandable, quotation-to-purchase flow was useful, credit note creation could be simple, and connected data reduced the need for repeated entry. Improvement feedback focused mainly on visual clarity and consistency.

Table~\ref{tab:user-feedback-themes} groups comments with similar meaning so that the most common positive points and improvement areas can be reviewed together.

\begin{table}[H]
	\centering
	\BlackbookTableSetup
	\caption{User Feedback Themes}
	\label{tab:user-feedback-themes}
	\begin{tabularx}{\textwidth}{|B{3.5cm}|Z|}
		\hline
		\textbf{Theme} & \textbf{Summary of Feedback} \\ \hline
		Easy areas & Users found purchase status, quotation creation, quotation-to-purchase flow, and connected data flow useful and understandable. \\ \hline
		Difficult areas & Some users had difficulty finding buttons, especially save buttons, detail buttons, status buttons, and finance-related options such as billing notes or credit notes. \\ \hline
		Visual clarity & Several comments suggested stronger colors, clearer status indicators, and more obvious emphasis for important actions or warning values. \\ \hline
		Workflow consistency & Some users felt that clickable rows and save-button placement should be more consistent across pages. \\ \hline
		Information density & Some users felt that certain pages, such as dashboard charts or cost information sections, contained many details and should be simplified or visually reorganized. \\ \hline
	\end{tabularx}
\end{table}

The recorded feedback identifies the main improvement areas as button visibility, status clarity, finance-action discoverability, and information density.

\section{Limitations Found During Development}
The system is functional, but some limitations remain:

\begin{itemize}
	\item Backend-wide authentication is configurable, so the final deployment setting must be checked before production use.
	\item User access is implemented with role and permission management, but production deployment should still review the final staff roles, permission assignments, and administrator accounts before real company use.
	\item The frontend still keeps mock-data fallback behavior for demonstration, so final evaluation should use backend-connected data.
	\item Large-scale production performance testing with many users and a very large database was not included in the current codebase.
	\item User testing was conducted with 10 participants, so the result is useful for project evaluation but not enough to represent all possible SME users.
\end{itemize}
