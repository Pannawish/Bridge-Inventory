# Set this for local development or a full-stack deployment.
# Leave it unset for a frontend-only preview deployment so the app stays in mock fallback mode.
VITE_API_BASE_URL=http://127.0.0.1:8000/api
